-- Auto-loaded by Neovim. Sets up default keymaps if not configured manually.
if vim.g.metroscope_loaded then
  return
end
vim.g.metroscope_loaded = true
-- Users should call require("metroscope").setup() in their config.
-- This file just ensures the plugin directory is recognized.
